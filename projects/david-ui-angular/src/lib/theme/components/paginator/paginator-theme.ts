import { IPropsMapper } from '../../../types/generic';

export const PaginatorTheme: IPropsMapper<object> = {
  container: {
    position: 'relative',
    display: 'flex',
    alignItems: 'items-center',
    cursor: 'cursor-pointer',
    gap:'gap-8'
  },

};
