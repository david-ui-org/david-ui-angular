import { IPropsMapper } from '../../../types/generic';

export const ButtonGroupTheme: IPropsMapper<object> = {
    initial: {
        display: "flex",
        flexDirection: "row",
      },
      fullWidth: {
        width: "w-full",
      },
};
