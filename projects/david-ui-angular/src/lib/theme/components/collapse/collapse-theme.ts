import { IPropsMapper } from '../../../types/generic';

export const CollapseTheme: IPropsMapper<object> = {
  collapse: {
    display: 'block',
    width: 'w-full',
    basis: 'basis-full',
    overflow: 'overflow-hidden',
  },
};
