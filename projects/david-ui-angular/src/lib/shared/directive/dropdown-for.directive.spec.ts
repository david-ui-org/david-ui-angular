import { DropdownForDirective } from './dropdown-for.directive';

describe('DropdownForDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownForDirective();
    expect(directive).toBeTruthy();
  });
});
